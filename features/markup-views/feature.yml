description: Adds support for Markup Views to the project
build:
    plugins:
        - org.grails.plugins.views-markup
dependencies:
    - scope: build
      coords: "org.grails.plugins:views-gradle:2.0.0.BUILD-SNAPSHOT"
    - scope: compile
      coords: "org.grails.plugins:views-markup"
