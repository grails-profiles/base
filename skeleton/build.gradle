buildscript {
    repositories {
@buildRepositories@
    }
    dependencies {
@buildDependencies@
    }
}

version "0.1"
group "@grails.app.group@"

@buildPlugins@

repositories {
@repositories@
}

dependencies {
@dependencies@
}

bootRun {
    jvmArgs('-Dspring.output.ansi.enabled=always')
}
